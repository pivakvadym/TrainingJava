package collections;

import java.util.*;

/**
 * @Autor: Vadym PIVAK
 * Date: 04/11/2020 10:40
 */
public class TestDiffJavaCollections {

    public static void main(String[] args) {

/**     What happens if we remove next() method in following code and why it will not work
 *
        List list = new ArrayList<>();
        list.addAll(Arrays.asList("A","B","C","D","E",1,2,3,4));
        Iterator it = list.iterator();
        StringBuilder sb = new StringBuilder();
        int i = 0;
        while(it.hasNext()){
            sb.append(it.next());
        }

        System.out.print(sb);

*/




    }

}
